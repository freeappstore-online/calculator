{
  "name": "calculator",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=22" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild"] },
  "scripts": {
    "dev": "pnpm --filter @calculator/web dev",
    "build": "pnpm --filter @calculator/web build",
    "preview": "pnpm --filter @calculator/web preview",
    "typecheck": "pnpm --filter @calculator/web exec tsc -b"
  }
}
